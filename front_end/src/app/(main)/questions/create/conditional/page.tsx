"use client";

import QuestionForm from "../../components/question_form";

const QuestionConditionalCreator: React.FC = ({}) => {
  return <QuestionForm question_type={"conditional"}></QuestionForm>;
};

export default QuestionConditionalCreator;
