"use client";
import { FC } from "react";

import { Tournament } from "@/types/projects";

type Props = {
  items: Tournament[];
};

const TournamentsList: FC<Props> = ({ items }) => {
  return <></>;
};

export default TournamentsList;
