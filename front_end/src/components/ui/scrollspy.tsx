"use client";

import ScrollSpyPrimitive from "react-scrollspy-navigation";

export function ScrollSpy(
  props: React.ComponentProps<typeof ScrollSpyPrimitive>
) {
  return <ScrollSpyPrimitive {...props} />;
}
